---
phase: 05.1-tune-opencodeagent-for-multi-step-tool-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/opencode/opencode.skill-eval-agent.json
  - src/agents/opencode/qwen3-4b-opencode-agent.Modelfile
  - src/agents/opencode/qwen3.5-4b-opencode-agent.Modelfile
  - src/agents/opencode/index.ts
  - tests/opencode-agent.test.ts
autonomous: true
requirements: [AGENT-03, AGENT-04]

must_haves:
  truths:
    - "opencode.json denies glob, grep, list, task, todowrite, skill -- leaving only bash, read, edit, write visible to the model"
    - "Qwen3:4b Modelfile exists with research-recommended parameters (num_ctx 8192, temperature 0, num_thread 8)"
    - "Qwen 3.5:4b bug is confirmed (tool calls printed as text, not executed) via a single screening trial"
    - "Qwen3:4b screening trial produces at least one successful tool call through opencode"
  artifacts:
    - path: "src/agents/opencode/opencode.skill-eval-agent.json"
      provides: "Aggressive tool denial config"
      contains: '"task": "deny"'
    - path: "src/agents/opencode/qwen3-4b-opencode-agent.Modelfile"
      provides: "Qwen3 4B model with tuned parameters"
      contains: "FROM qwen3:4b"
    - path: "src/agents/opencode/index.ts"
      provides: "Updated OPENCODE_MODEL and prompt prefix"
      contains: "qwen3-4b-opencode-agent"
  key_links:
    - from: "src/agents/opencode/index.ts"
      to: "src/agents/opencode/opencode.skill-eval-agent.json"
      via: "fs.readFileSync config injection"
      pattern: "opencode\\.skill-eval-agent\\.json"
    - from: "src/agents/opencode/opencode.skill-eval-agent.json"
      to: "Ollama model registry"
      via: "model field in provider.ollama.models"
      pattern: "qwen3-4b-opencode-agent"
---

<objective>
Update OpenCodeAgent config and code to use research-recommended settings: aggressive tool denial (10 -> 5 tools), Qwen3-family Modelfiles, and improved prompt prefix. Confirm the Qwen 3.5 Ollama bug with a single trial (per user's "start with qwen3.5:4b" decision), then screen Qwen3:4b.

Purpose: Establish the tuning foundation. Research identified Qwen 3.5 tool calling is broken on Ollama (4 open bugs) and tool count overwhelm as root causes. This plan applies both fixes and runs initial screening.
Output: Updated config/code, two Modelfiles, Qwen 3.5 bug confirmation, Qwen3:4b screening result.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/05.1-CONTEXT.md
@.planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/05.1-RESEARCH.md
@.planning/phases/05-opencodeagent/05-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/agents/opencode/index.ts:
```typescript
const OPENCODE_MODEL = 'qwen3.5-4b-opencode-agent'; // Line 14 -- change to qwen3-4b-opencode-agent
```

Lines 101-108 -- prompt prefix (tuning surface):
```typescript
const prefixedInstruction = [
    'IMPORTANT: You MUST use the Bash tool to run ALL shell commands described below.',
    'Do NOT stop after the first command. Execute EVERY command in order.',
    'Do NOT summarize results between commands. Just run the next command immediately.',
    'Do NOT use Skills or any feature other than Bash, Read, and Edit.',
    '/no_think\n',
    instruction,
].join('\n');
```

From src/agents/opencode/opencode.skill-eval-agent.json:
```json
{
  "model": "ollama/qwen3.5-4b-opencode-agent",
  "permission": {
    "read": "allow", "edit": "allow", "bash": "allow",
    "glob": "allow", "grep": "allow", "list": "allow",
    "webfetch": "deny", "websearch": "deny", "codesearch": "deny",
    "external_directory": "deny", "doom_loop": "allow"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config, Modelfiles, code, and tests for Qwen3 + tool denial</name>
  <files>
    src/agents/opencode/opencode.skill-eval-agent.json,
    src/agents/opencode/qwen3-4b-opencode-agent.Modelfile,
    src/agents/opencode/qwen3.5-4b-opencode-agent.Modelfile,
    src/agents/opencode/index.ts,
    tests/opencode-agent.test.ts
  </files>
  <action>
    **opencode.skill-eval-agent.json** -- Apply aggressive tool denial from research. The final config should be:

    ```json
    {
      "$schema": "https://opencode.ai/config.json",
      "provider": {
        "ollama": {
          "npm": "@ai-sdk/openai-compatible",
          "name": "Ollama (local)",
          "options": {
            "baseURL": "http://localhost:11434/v1"
          },
          "models": {
            "qwen3-4b-opencode-agent": {
              "name": "Qwen3 4B OpenCode Agent",
              "tools": true
            }
          }
        }
      },
      "model": "ollama/qwen3-4b-opencode-agent",
      "permission": {
        "read": "allow",
        "edit": "allow",
        "bash": "allow",
        "write": "allow",
        "glob": "deny",
        "grep": "deny",
        "list": "deny",
        "task": "deny",
        "todowrite": "deny",
        "skill": "deny",
        "webfetch": "deny",
        "websearch": "deny",
        "codesearch": "deny",
        "external_directory": "deny",
        "doom_loop": "allow"
      }
    }
    ```

    Key changes: (1) model -> qwen3-4b-opencode-agent, (2) add "write": "allow", (3) deny glob/grep/list/task/todowrite/skill (was allow), (4) model name in provider.ollama.models updated.

    **qwen3-4b-opencode-agent.Modelfile** -- Create new file at `src/agents/opencode/`:

    ```
    # Modelfile for the opencode agent variant of qwen3:4b.
    #
    # Qwen3 uses the stable Hermes-style tool calling format on Ollama.
    # Qwen 3.5 has broken tool calling on Ollama (issues #14493, #14745).
    # NO custom SYSTEM prompt -- opencode provides its own.
    #
    # To create: ollama create qwen3-4b-opencode-agent -f qwen3-4b-opencode-agent.Modelfile

    FROM qwen3:4b
    PARAMETER num_ctx 8192
    PARAMETER num_predict 4096
    PARAMETER temperature 0
    PARAMETER num_batch 1024
    PARAMETER num_thread 8
    ```

    **qwen3.5-4b-opencode-agent.Modelfile** -- Update num_ctx from 4096 to 8192 (research: opencode system prompt + tool defs need more context). Keep all other params. This Modelfile is used only for the bug confirmation test.

    **index.ts** -- Two changes:
    1. Change `OPENCODE_MODEL` constant (line 14) from `'qwen3.5-4b-opencode-agent'` to `'qwen3-4b-opencode-agent'`. Update the JSDoc comment above it to explain Qwen3 is used because Qwen 3.5 has broken tool calling on Ollama (issues #14493, #14745).
    2. Update the prompt prefix (lines 101-108) to:
       ```typescript
       const prefixedInstruction = [
           'CRITICAL: Execute ALL commands below in order. After each command completes, immediately run the next one. Do NOT stop, summarize, or explain between commands.',
           'You have 4 tools: Bash, Read, Edit, Write. Use Bash for ALL shell commands.',
           'After the last command, respond with a one-line summary.',
           '/no_think\n',
           instruction,
       ].join('\n');
       ```
       This removes the Skill mention (skill is now denied, model never sees it) and adds stronger multi-step reinforcement per research recommendation.

    **tests/opencode-agent.test.ts** -- Update all assertions referencing the model name from `qwen3.5-4b-opencode-agent` to `qwen3-4b-opencode-agent`. Update any permission assertions to match the new deny list (glob, grep, list, task, todowrite, skill all denied).
  </action>
  <verify>
    <automated>npx jest --testPathPattern opencode-agent -- --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    All opencode-agent tests pass with new model name and permission config. Config JSON has 8 denied tools. Qwen3 4B Modelfile exists. Prompt prefix uses research-recommended wording.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bug confirmation trial (Qwen 3.5) + Qwen3:4b screening trial</name>
  <files>
    .planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/experiments/01-qwen35-bug-confirmation.md,
    .planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/experiments/02-qwen3-4b-screening.md
  </files>
  <action>
    This task runs two screening experiments via CLI. Record results in structured markdown (same format as Phase 4.1).

    **Experiment 1: Qwen 3.5 bug confirmation** (honoring user's "start with qwen3.5:4b" decision)

    1. Temporarily revert OPENCODE_MODEL in index.ts to `'qwen3.5-4b-opencode-agent'` and the model field in opencode.skill-eval-agent.json to `"ollama/qwen3.5-4b-opencode-agent"` (also update models key). Keep the new permission deny list in place.
    2. Ensure the qwen3.5:4b base model is pulled: `ollama pull qwen3.5:4b`
    3. Create the model variant: `ollama create qwen3.5-4b-opencode-agent -f src/agents/opencode/qwen3.5-4b-opencode-agent.Modelfile`
    4. Run 1 warmup: `npx tsx src/cli.ts eval --task superlint_demo --provider local --agent opencode` (discard result)
    5. Run 1 screening trial: same command, capture output
    6. Record result: model, config notes (8 denied tools + num_ctx 8192), duration, reward, tool calls observed, failure mode
    7. Stop the model: `ollama stop qwen3.5-4b-opencode-agent`
    8. Write results to `experiments/01-qwen35-bug-confirmation.md`

    Expected outcome: tool calls printed as text (not executed) per Ollama issue #14493, confirming the bug. If it somehow works (reward >= 0.85), that is an even better outcome -- document and proceed.

    **Experiment 2: Qwen3:4b screening**

    1. Restore OPENCODE_MODEL to `'qwen3-4b-opencode-agent'` and opencode.skill-eval-agent.json model to `"ollama/qwen3-4b-opencode-agent"` (restore models key too).
    2. Pull qwen3:4b: `ollama pull qwen3:4b`
    3. Create the model variant: `ollama create qwen3-4b-opencode-agent -f src/agents/opencode/qwen3-4b-opencode-agent.Modelfile`
    4. Run 1 warmup, then 1 screening trial: `npx tsx src/cli.ts eval --task superlint_demo --provider local --agent opencode`
    5. Record result in same format to `experiments/02-qwen3-4b-screening.md`
    6. Stop the model: `ollama stop qwen3-4b-opencode-agent`

    **IMPORTANT:** Each eval run takes ~4-5 min. Use `run_in_background: true` for the eval commands. NEVER poll output files -- wait for the completion notification. Between experiments, `ollama stop <model>` to free memory.

    After both experiments, ensure index.ts and opencode.skill-eval-agent.json are left in the Qwen3:4b state (the default going forward). The code committed at the end of this plan should reference qwen3-4b-opencode-agent.
  </action>
  <verify>
    <automated>git diff --name-only HEAD 2>/dev/null; cat .planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/experiments/01-qwen35-bug-confirmation.md .planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/experiments/02-qwen3-4b-screening.md 2>/dev/null | head -40</automated>
  </verify>
  <done>
    Two experiment result files exist with structured benchmark data (model, config, duration, reward, tool calls, notes). Qwen 3.5 bug status confirmed. Qwen3:4b screening result captured. Code left in Qwen3:4b state. If Qwen3:4b scores >= 0.85, Plan 02 can move directly to 3-trial validation.
  </done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern opencode-agent` passes
- `src/agents/opencode/opencode.skill-eval-agent.json` contains 8 denied tools
- `src/agents/opencode/qwen3-4b-opencode-agent.Modelfile` exists with num_ctx 8192
- Two experiment result files exist in `experiments/` directory
- index.ts references `qwen3-4b-opencode-agent`
</verification>

<success_criteria>
Config foundation applied (tool denial + Qwen3 Modelfile + prompt prefix). Qwen 3.5 bug confirmed or disproven with structured data. Qwen3:4b screening result available to inform Plan 02 experiment strategy.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/05.1-01-SUMMARY.md`
</output>
