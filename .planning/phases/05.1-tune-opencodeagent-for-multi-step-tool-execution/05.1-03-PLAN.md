---
phase: 05.1-tune-opencodeagent-for-multi-step-tool-execution
plan: 03
type: execute
wave: 3
depends_on: ["05.1-02"]
files_modified:
  - src/agents/opencode/index.ts
  - .planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/experiments/04-docker-validation.md
  - .planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/experiments/05-local-3trial-final.md
autonomous: false
requirements: [AGENT-03, AGENT-04]

must_haves:
  truths:
    - "OpenCodeAgent completes superlint_demo with reward >= 0.85 on Docker provider"
    - "OpenCodeAgent completes superlint_demo with reward >= 0.85 on local provider (3-trial validated)"
    - "Both providers produce structured benchmark data"
  artifacts:
    - path: ".planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/experiments/04-docker-validation.md"
      provides: "Docker provider experiment results"
      contains: "reward"
    - path: ".planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/experiments/05-local-3trial-final.md"
      provides: "Local 3-trial final validation (if not already done in Plan 02)"
      contains: "reward"
  key_links:
    - from: "src/agents/opencode/index.ts"
      to: "Docker container"
      via: "host.docker.internal baseURL adjustment"
      pattern: "host\\.docker\\.internal"
    - from: "opencode.skill-eval-agent.json"
      to: "Docker Ollama"
      via: "baseURL rewrite in OpenCodeAgent.run()"
---

<objective>
Validate the winning model+config on the Docker provider and confirm the local provider 3-trial result. This closes Phase 5.1 by proving both providers meet the 0.85 reward threshold.

Purpose: Phase 5.1 requires both local AND Docker providers to pass. Docker runs Linux ARM64 natively on WSL2-backed Docker Desktop -- no QEMU overhead. The SIGABRT issue #13367 should not affect us (4KB page size) but needs verification.
Output: Docker validation results, final local 3-trial confirmation, phase closure data.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/05.1-CONTEXT.md
@.planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/05.1-RESEARCH.md
@.planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/05.1-01-SUMMARY.md
@.planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/05.1-02-SUMMARY.md

<interfaces>
From src/agents/opencode/index.ts:
- Docker detection (lines 55-70): checks /proc/1/cgroup and /workspace path
- Docker baseURL rewrite (line 73): configJson.provider.ollama.options.baseURL = 'http://host.docker.internal:11434/v1'
- Docker opencode install (lines 76-85): npm install -g opencode-ai if not present
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker provider screening and validation</name>
  <files>
    .planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/experiments/04-docker-validation.md
  </files>
  <action>
    Test the winning configuration from Plan 02 on the Docker provider.

    **Pre-flight checks:**
    1. Verify Docker Desktop is running and WSL2 backend is active
    2. Verify Ollama is accessible from Docker: the OpenCodeAgent already rewrites baseURL to `host.docker.internal:11434` for Docker context
    3. Ensure the winning model is loaded in Ollama (same model used in Plan 02)

    **Docker screening:**
    1. Run 1 warmup: `npx tsx src/cli.ts eval --task superlint_demo --provider docker --agent opencode`
    2. Run 1 screening trial (same command, capture output)
    3. Record: model, provider (docker), duration, reward, tool calls, any Docker-specific issues (SIGABRT, opencode install, baseURL connectivity)

    Use `run_in_background: true` for eval commands. Docker trials may take longer due to container startup and opencode npm install on first run.

    **If screening passes (reward >= 0.85):**
    - Run 3-trial validation (1 warmup + 3 measured)
    - Record all results

    **If Docker fails with SIGABRT (exit 134):**
    - This is the linux-arm64 issue #13367. Research says WSL2 uses 4KB pages so it shouldn't occur, but if it does:
    - Document the failure
    - Note that Docker validation is blocked pending opencode fix
    - The plan can still close with local provider passing if Docker is a known platform issue

    **If Docker fails with connectivity issues:**
    - Verify `host.docker.internal` resolves inside container
    - Check Ollama is listening on 0.0.0.0:11434 (not just localhost)
    - Set OLLAMA_HOST=0.0.0.0 if needed and retry

    **If Docker fails with model capability issues (same tool-calling problems as local):**
    - Try the same tuning variations from Plan 02 (prompt, num_ctx, etc.)
    - Document each attempt

    After Docker experiments, stop the model: `ollama stop <winning-model>`

    Write all results to `experiments/04-docker-validation.md` in structured markdown table format.
  </action>
  <verify>
    <automated>cat .planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/experiments/04-docker-validation.md 2>/dev/null | head -40</automated>
  </verify>
  <done>
    Docker provider results documented. Either: (a) reward >= 0.85 on 3-trial validation, or (b) failure documented with root cause and whether it is a platform issue vs model capability issue.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User approval of Phase 5.1 results</name>
  <action>
    Present experiment results from all plans for user review. No automated work -- this is a verification gate.
  </action>
  <verify>
    <automated>echo "Checkpoint -- requires human approval"</automated>
  </verify>
  <done>User has reviewed and approved the Phase 5.1 results, or provided feedback for follow-up.</done>
  <what-built>
    OpenCodeAgent tuned to complete superlint_demo multi-step workflow. Experiments run on both local and Docker providers with structured benchmark data.
  </what-built>
  <how-to-verify>
    1. Review experiment results in `.planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/experiments/`
    2. Verify the winning model+config makes sense (check model name, parameters, prompt prefix)
    3. Optionally run a quick manual trial: `npx tsx src/cli.ts eval --task superlint_demo --provider local --agent opencode`
    4. Check that both local and Docker meet the 0.85 threshold (or that Docker failures are documented as platform issues)
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5.1, or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
- Docker experiment results exist in `experiments/04-docker-validation.md`
- Both providers have structured benchmark data showing reward >= 0.85
- All tests pass: `npx jest --testPathPattern opencode-agent`
- index.ts and opencode.json reference the winning model consistently
</verification>

<success_criteria>
Both local and Docker providers achieve reward >= 0.85 with 3-trial validation (or Docker failure documented as platform issue). User approves the results. Phase 5.1 can be closed.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-tune-opencodeagent-for-multi-step-tool-execution/05.1-03-SUMMARY.md`
</output>
