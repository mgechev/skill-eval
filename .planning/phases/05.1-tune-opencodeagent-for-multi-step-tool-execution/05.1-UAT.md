---
status: complete
phase: 05.1-tune-opencodeagent-for-multi-step-tool-execution
source: 05.1-01-SUMMARY.md, 05.1-02-SUMMARY.md, 05.1-03-SUMMARY.md
started: 2026-03-13T12:00:00Z
updated: 2026-03-13T15:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. OpenCode agent completes eval with Docker provider
expected: Run `npx tsx src/cli.ts superlint_demo --agent=opencode --provider=docker --trials=1`. The agent uses qwen3:4b model, executes the superlint_demo task via Docker, and the final reward is >= 0.85. No SIGSEGV crashes. LLM rubric score is > 0.
result: pass

### 2. OpenCode agent completes eval with Local provider
expected: Run `npx tsx src/cli.ts superlint_demo --agent=opencode --provider=local --trials=1`. The agent uses qwen3:4b model, executes the superlint_demo task locally, and the final reward is >= 0.85. No SIGSEGV crashes. LLM rubric score is > 0.
result: pass

### 3. Default opencode model naming convention
expected: The OPENCODE_MODEL constant in `src/agents/opencode/index.ts` should follow the naming convention `qwen3-4b-skill-eval-opencode-agent` (includes `skill-eval` to match project conventions).
result: issue
reported: "The model should be called qwen3-4b-skill-eval-opencode-agent"
severity: minor

### 4. Grader memory contention fix works
expected: After the agent finishes, the agent model is evicted (keep_alive:0 + polling ollama ps) before the grader loads. Grader uses num_ctx 2048 (not 8192). This prevents llm_rubric 0.0 timeout scores. Verified by: both Docker and Local trials above producing LLM rubric > 0.
result: pass

### 5. Prompt prefix includes description field hint
expected: In `src/agents/opencode/index.ts`, the prompt prefix sent to the model includes guidance about including the `description` field in bash tool calls. This was the critical fix that took reward from 0.00 to 0.97.
result: pass

### 6. OPENCODE_DISABLE_* env flags are set
expected: The opencode agent run command sets OPENCODE_DISABLE_CLAUDE_CODE_PROMPT, OPENCODE_DISABLE_PROJECT_CONFIG, and OPENCODE_DISABLE_EXTERNAL_SKILLS environment variables to keep the context window clean for the small 1.7B-4B models.
result: pass

### 7. LLM grader scores empty transcript low
expected: Feed the LLM grader an empty/minimal session transcript (no commands executed, no agent output). The grader should return a score significantly below 1.0 (ideally 0.0-0.3), proving it discriminates rather than rubber-stamping 1.0.
result: pass

### 8. LLM grader scores wrong-approach transcript low
expected: Feed the LLM grader a transcript where the agent skipped the mandatory workflow (e.g., manually edited app.js instead of using `superlint fix`, never ran `superlint verify`). The grader should return a score below 0.5, proving it catches workflow violations.
result: pass

### 9. OllamaAgent model naming convention
expected: The OllamaToolAgent default model in `src/agents/ollama/types.ts` should be named `qwen2.5-3b-skill-eval-ollama-agent` to distinguish it from the opencode agent models and follow a consistent naming pattern.
result: issue
reported: "OllamaAgent model is called qwen2.5-3b-skill-eval-agent, should include -ollama- to distinguish from opencode agent models"
severity: minor

### 10. Dead qwen2.5-3b-opencode-agent model and Modelfile
expected: No unused/dead model references should exist. The `qwen2.5-3b-opencode-agent` Modelfile under `src/agents/opencode/` and the pulled Ollama model are leftovers -- the OpenCodeAgent uses `qwen3-4b-opencode-agent` now.
result: issue
reported: "qwen2.5-3b-opencode-agent is dead code -- Modelfile exists in src/agents/opencode/ and model is pulled in Ollama but nothing references it"
severity: minor

### 11. LLM grader partial-credit calibration
expected: The LLM grader (qwen2.5:3b) should produce calibrated partial scores for transcripts that partially follow the workflow. Tested 8 variants: (1) check+fix no verify, (2) fix+verify no check, (3) all 3 steps + eslint, (4) correct but inefficient, (5) only verify, (6) ideal perfect run, (7) check+sed+fix+verify mixed, (8) only check gave up. Scores should correlate with actual workflow compliance.
result: issue
reported: "Grader has good binary discrimination (0.0 for empty/wrong, 1.0 for perfect) but poor partial-credit calibration. Key problems: (1) Hallucinated steps -- scored 0.9 for run that skipped check, 0.7 for run that only did verify. (2) Ceiling too low -- ideal perfect run only scored 0.7. (3) Inconsistent penalties -- eslint penalized more than skipping check. (4) Reasoning frequently contradicts transcript content. Full results: check+fix no verify=0.0, fix+verify no check=0.9, all 3+eslint=0.2, correct but inefficient=0.4, only verify=0.7, ideal perfect=0.7, check+sed+fix+verify=0.2, only check=0.0"
severity: major

## Summary

total: 11
passed: 7
issues: 4
pending: 0
skipped: 0

## Gaps

- truth: "Default opencode model constant should follow skill-eval naming convention"
  status: closed
  reason: "Fixed in Plan 05.1-04: renamed to qwen3-4b-skill-eval-opencode-agent"
  severity: minor
  test: 3
  closed_by: 05.1-04

- truth: "OllamaAgent model name should include -ollama- to distinguish from opencode agent"
  status: closed
  reason: "Fixed in Plan 05.1-04: renamed to qwen2.5-3b-skill-eval-ollama-agent"
  severity: minor
  test: 9
  closed_by: 05.1-04

- truth: "No dead model artifacts should exist in the codebase"
  status: closed
  reason: "Fixed in Plan 05.1-04: dead Modelfiles already removed in prior work, confirmed clean"
  severity: minor
  test: 10
  closed_by: 05.1-04

- truth: "LLM grader should produce calibrated partial scores that correlate with actual workflow compliance"
  status: closed
  reason: "Fixed in Plan 05.1-05: upgraded to qwen3:4b with binary checklist decomposition (CheckEval/RocketEval). 11/12 calibration variants in expected range, real trial reward 1.00."
  severity: major
  test: 11
  closed_by: 05.1-05
