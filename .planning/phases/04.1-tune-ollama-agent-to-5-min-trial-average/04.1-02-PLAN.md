---
phase: 04.1-tune-ollama-agent-to-5-min-trial-average
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - modelfiles/qwen3-skill-eval-agent.Modelfile
  - modelfiles/qwen3-skill-eval-agent.ci.Modelfile
  - src/agents/ollama/index.ts
  - src/agents/ollama/types.ts
  - .planning/phases/04.1-tune-ollama-agent-to-5-min-trial-average/benchmark-results/experiments.md
autonomous: false
requirements:
  - TUNE-PARAMS
  - TUNE-PROMPT
  - TUNE-AGENTCFG
must_haves:
  truths:
    - "Temperature, num_predict, and system prompt are tuned based on measured experiment results"
    - "maxIterations is set based on actual turn count data from profiling"
    - "Best qwen3:4b configuration is identified and applied to both local and CI Modelfiles"
    - "Experiment results document records each experiment with before/after data"
  artifacts:
    - path: "modelfiles/qwen3-skill-eval-agent.Modelfile"
      provides: "Tuned local Modelfile with best parameters"
      contains: "FROM qwen3:4b"
    - path: "modelfiles/qwen3-skill-eval-agent.ci.Modelfile"
      provides: "Tuned CI Modelfile (same params, different thread count)"
      contains: "FROM qwen3:4b"
    - path: "src/agents/ollama/index.ts"
      provides: "Updated system prompt"
      contains: "SYSTEM_PROMPT"
    - path: ".planning/phases/04.1-tune-ollama-agent-to-5-min-trial-average/benchmark-results/experiments.md"
      provides: "All experiment results with verdicts"
      contains: "## Experiment"
  key_links:
    - from: "modelfiles/qwen3-skill-eval-agent.Modelfile"
      to: "ollama create qwen3-skill-eval-agent"
      via: "Modelfile used to create/update the agent model"
      pattern: "FROM qwen3"
    - from: "src/agents/ollama/index.ts"
      to: "Ollama chat API"
      via: "SYSTEM_PROMPT sent as system message in messages array"
      pattern: "SYSTEM_PROMPT"
---

<objective>
Systematically tune qwen3:4b parameters, system prompt, and agent configuration to minimize trial duration while maintaining 0.90+ reward.

Purpose: The current config (~440-590s/trial) needs to reach 300s average. Research identified temperature, num_predict, system prompt, and maxIterations as highest-impact tuning knobs. Each change is tested in isolation, then the best combination is applied.

Output: Tuned Modelfiles (local + CI), updated system prompt, adjusted agent config, and experiment results documenting each change's impact.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-tune-ollama-agent-to-5-min-trial-average/04.1-RESEARCH.md
@.planning/phases/04.1-tune-ollama-agent-to-5-min-trial-average/04.1-01-SUMMARY.md

@src/agents/ollama/index.ts
@src/agents/ollama/types.ts
@src/agents/ollama/tools.ts
@modelfiles/qwen3-skill-eval-agent.Modelfile
@modelfiles/qwen3-skill-eval-agent.ci.Modelfile
@scripts/benchmark-agent.ts

<interfaces>
<!-- Current system prompt (tuning target) -->
From src/agents/ollama/index.ts:32-35:
```typescript
const SYSTEM_PROMPT =
    'You are an AI agent that completes coding tasks. Use the provided tools to complete the task. ' +
    'Do not explain your reasoning - just call the appropriate tool. ' +
    'When you are done, respond with a summary of what you did. /no_think';
```

From src/agents/ollama/types.ts:45-50:
```typescript
export const DEFAULT_OLLAMA_AGENT_CONFIG: OllamaAgentConfig = {
    model: 'qwen3-skill-eval-agent',
    host: 'http://localhost:11434',
    maxIterations: 30,
    maxToolOutputChars: 8000,
};
```

Current Modelfile (modelfiles/qwen3-skill-eval-agent.Modelfile):
```
FROM qwen3:4b
PARAMETER num_ctx 4096
PARAMETER num_predict 4096
PARAMETER temperature 0
PARAMETER num_thread 8
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run parameter and prompt experiments</name>
  <files>modelfiles/qwen3-skill-eval-agent.Modelfile, src/agents/ollama/index.ts, src/agents/ollama/types.ts, .planning/phases/04.1-tune-ollama-agent-to-5-min-trial-average/benchmark-results/experiments.md</files>
  <action>
Run experiments in priority order from the research. Change ONE variable per experiment. Use the benchmark script from Plan 01 for each experiment.

**Experiment sequence (run in this order, stop early if target met):**

1. **Temperature 0.25** -- Create temp Modelfile with `temperature 0.25` (all else same). Run benchmark: `npx ts-node scripts/benchmark-agent.ts --name temp-025 --modelfile modelfiles/qwen3-skill-eval-agent.experiment.Modelfile --trials 3`. Record results.

2. **Structured system prompt** -- Update SYSTEM_PROMPT in `src/agents/ollama/index.ts` to:
   ```
   You are a coding agent. Complete the task using the provided tools.

   Rules:
   - Call tools directly, no explanations between calls.
   - Read files before editing them.
   - If a command fails, try a different approach instead of repeating it.
   - When finished, respond with a brief summary.

   /no_think
   ```
   Run benchmark with `--name structured-prompt`. Revert the source change after benchmarking (use git checkout to restore original).

3. **num_predict 2048** -- Create temp Modelfile with `num_predict 2048`. Run benchmark with `--name predict-2048`. Check if any tool calls were truncated (reward drop is the signal).

4. **maxIterations 15** -- Temporarily edit DEFAULT_OLLAMA_AGENT_CONFIG.maxIterations to 15 in `src/agents/ollama/types.ts`. Run benchmark with `--name max-iter-15`. Check if agent completes (reward same as baseline). Revert after.

5. **num_batch 1024** -- Add `PARAMETER num_batch 1024` to temp Modelfile. Run benchmark with `--name batch-1024`.

6. **OLLAMA_FLASH_ATTENTION + KV cache** -- Do NOT restart Ollama from this script. Instead, document that this experiment requires restarting Ollama with env vars and should be run manually:
   ```bash
   # Stop Ollama, restart with: OLLAMA_FLASH_ATTENTION=1 OLLAMA_KV_CACHE_TYPE=q8_0 ollama serve
   npx ts-node scripts/benchmark-agent.ts --name flash-attn --trials 3
   ```
   If the executor can restart Ollama (on Windows: `taskkill.exe //IM ollama.exe //F` then start with env vars), do so. Otherwise, skip and document as "requires manual Ollama restart".

7. **maxToolOutputChars 4000** -- Temporarily edit DEFAULT_OLLAMA_AGENT_CONFIG.maxToolOutputChars to 4000. Run benchmark with `--name tool-output-4000`. Revert after.

**For each experiment:**
- Record: experiment name, what changed, avg duration, std dev, avg reward, avg commands, verdict (KEEP/REJECT)
- KEEP criteria: avg reward >= 0.90 AND (duration improved OR commands reduced)
- REJECT criteria: reward < 0.90 OR duration increased

**After all experiments:**
- Identify the best combination of KEPT changes
- Apply the winning combination to `modelfiles/qwen3-skill-eval-agent.Modelfile` and `modelfiles/qwen3-skill-eval-agent.ci.Modelfile` (CI keeps `num_thread 3`, all other params same as local)
- Apply system prompt changes to `src/agents/ollama/index.ts` if the structured prompt was KEPT
- Apply maxIterations/maxToolOutputChars changes to `src/agents/ollama/types.ts` if KEPT
- Run a final benchmark with the combined config: `--name best-combined --trials 3`

**Write experiments.md** at `.planning/phases/04.1-tune-ollama-agent-to-5-min-trial-average/benchmark-results/experiments.md` with all results in the research template format. Include a summary table:

```markdown
| Experiment | Avg Duration (s) | StdDev (s) | Avg Reward | Avg Cmds | Verdict |
|-----------|-----------------|-----------|-----------|---------|---------|
| baseline | {val} | {val} | {val} | {val} | BASELINE |
| temp-025 | {val} | {val} | {val} | {val} | KEEP/REJECT |
| ... | ... | ... | ... | ... | ... |
| best-combined | {val} | {val} | {val} | {val} | FINAL |
```

Important constraints:
- Do NOT use Modelfile SYSTEM directive (known Ollama bug -- keep system prompt in API messages only)
- Do NOT modify task instruction files
- Keep 8 threads locally, 3 in CI
- After ALL experiments, restore the Modelfile from the experiment version and rebuild: `ollama create qwen3-skill-eval-agent -f modelfiles/qwen3-skill-eval-agent.Modelfile`
  </action>
  <verify>
    <automated>test -f ".planning/phases/04.1-tune-ollama-agent-to-5-min-trial-average/benchmark-results/experiments.md" && echo "experiments exist"</automated>
  </verify>
  <done>Experiments document exists with results for each tested parameter. Best combined config is applied to Modelfiles, system prompt, and agent config. Final benchmark run with combined config is documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify experiment results and decide on escalation</name>
  <action>
Review the experiment results and determine whether the 5-minute target is met.

What was built: Systematic parameter tuning of qwen3:4b with benchmark results for each experiment. Best combined configuration applied to production files (Modelfiles, system prompt, agent config).

How to verify:
1. Review `.planning/phases/04.1-tune-ollama-agent-to-5-min-trial-average/benchmark-results/experiments.md` -- check that each experiment has clear before/after data
2. Check if the "best-combined" final benchmark meets the 5-min (300s) target
3. If target met: Phase 4.1 is complete (Plan 03 can be skipped)
4. If target NOT met: Note the gap (e.g., "best is 380s, need 300s") -- Plan 03 escalation needed
5. Verify reward >= 0.90 across all trials in the best-combined run
6. Run a quick manual trial to confirm: `npm run eval -- superlint_demo --agent=ollama --provider=local --trials=1`
  </action>
  <verify>User confirms experiment results</verify>
  <done>User responds "target-met" (skip Plan 03) or "escalate" (proceed to Plan 03)</done>
</task>

</tasks>

<verification>
- experiments.md contains results for temperature, system prompt, num_predict, maxIterations experiments (at minimum)
- Each experiment has avg duration, std dev, reward, commands, and verdict
- Best combined config is applied to Modelfiles and agent code
- Final "best-combined" benchmark was run with 3 trials
- All applied changes maintain reward >= 0.90
</verification>

<success_criteria>
- At least 5 experiments completed with documented results
- Best combined configuration identified and applied
- experiments.md provides clear data for each parameter change
- If 5-min target met: Phase 4.1 complete
- If 5-min target not met: Clear measurement of remaining gap for Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-tune-ollama-agent-to-5-min-trial-average/04.1-02-SUMMARY.md`
</output>
